[package]
name = "contract"
version = "0.1.0"
edition = "2021"
description = "Wire + bus contract for the modular embedded pentest implant engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
